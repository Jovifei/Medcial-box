# Local development defaults only. Replace before any shared deployment.
POSTGRES_DB=medbox
POSTGRES_USER=medbox
POSTGRES_PASSWORD=local-dev-only
API_PORT=3000

# Add model and drug-data API keys to a private server-side environment only.
# Never put provider keys in mini-program settings or commit them to source control.
